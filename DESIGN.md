---
name: "ระบบฐานข้อมูลด้านการศึกษา สำนักงานศึกษาธิการภาค 15"
description: "ระบบภาพสำหรับโต๊ะวางแผนการศึกษาระดับภูมิภาคที่เป็นทางการ ทันสมัย และเป็นระบบ"
colors:
  primary-action: "#0f766e"
  primary-bright: "#14b8a6"
  primary-active: "#115e59"
  primary-soft: "#f0fdfa"
  system-navy: "#1e293b"
  page-canvas: "#f8fafc"
  surface: "#ffffff"
  ink-strong: "#0f172a"
  ink-default: "#1e293b"
  ink-muted: "#475569"
  ink-subtle: "#64748b"
  border-soft: "#e2e8f0"
  border-control: "#7c8da3"
  data-blue: "#2563eb"
  data-amber: "#b45309"
  data-rose: "#e11d48"
  data-violet: "#7c3aed"
  data-cyan: "#0e7490"
typography:
  display:
    fontFamily: "ui-sans-serif, system-ui, sans-serif, 'Segoe UI', Tahoma"
    fontSize: "3rem"
    fontWeight: 700
    lineHeight: 1.18
    letterSpacing: "normal"
  headline:
    fontFamily: "ui-sans-serif, system-ui, sans-serif, 'Segoe UI', Tahoma"
    fontSize: "1.25rem"
    fontWeight: 700
    lineHeight: 1.5
    letterSpacing: "normal"
  title:
    fontFamily: "ui-sans-serif, system-ui, sans-serif, 'Segoe UI', Tahoma"
    fontSize: "1.125rem"
    fontWeight: 600
    lineHeight: 1.55
    letterSpacing: "normal"
  body:
    fontFamily: "ui-sans-serif, system-ui, sans-serif, 'Segoe UI', Tahoma"
    fontSize: "1rem"
    fontWeight: 400
    lineHeight: 1.625
    letterSpacing: "normal"
  label:
    fontFamily: "ui-sans-serif, system-ui, sans-serif, 'Segoe UI', Tahoma"
    fontSize: "0.875rem"
    fontWeight: 600
    lineHeight: 1.57
    letterSpacing: "0.025em"
  metadata:
    fontFamily: "ui-sans-serif, system-ui, sans-serif, 'Segoe UI', Tahoma"
    fontSize: "0.8125rem"
    fontWeight: 500
    lineHeight: 1.54
    letterSpacing: "normal"
rounded:
  nav: "6px"
  control: "8px"
  card: "12px"
  overlay: "16px"
  pill: "9999px"
spacing:
  xxs: "4px"
  xs: "8px"
  sm: "12px"
  md: "16px"
  lg: "20px"
  xl: "24px"
  2xl: "32px"
components:
  button-primary:
    backgroundColor: "{colors.primary-action}"
    textColor: "{colors.surface}"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "12px 20px"
  button-primary-hover:
    backgroundColor: "{colors.primary-active}"
    textColor: "{colors.surface}"
    rounded: "{rounded.control}"
    padding: "12px 20px"
  button-secondary:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.ink-muted}"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "12px 20px"
  input:
    backgroundColor: "{colors.page-canvas}"
    textColor: "{colors.ink-default}"
    typography: "{typography.body}"
    rounded: "{rounded.control}"
    padding: "10px 12px"
  card:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.ink-default}"
    rounded: "{rounded.card}"
    padding: "18px"
  status-chip:
    backgroundColor: "{colors.primary-soft}"
    textColor: "{colors.primary-active}"
    typography: "{typography.title}"
    rounded: "{rounded.pill}"
    padding: "6px 16px"
  nav-active:
    backgroundColor: "{colors.system-navy}"
    textColor: "{colors.primary-bright}"
    typography: "{typography.body}"
    rounded: "{rounded.nav}"
    padding: "8px 12px"
---

# Design System: ระบบฐานข้อมูลด้านการศึกษา สำนักงานศึกษาธิการภาค 15

## Overview

**Creative North Star: "โต๊ะวางแผนการศึกษาภูมิภาค"**

ระบบภาพเปรียบเสมือนโต๊ะทำงานที่นักวิชาการ นักวิเคราะห์ และผู้บริหารเปิดใช้ร่วมกัน ข้อมูลถูกจัดวางเป็นหมวดหมู่ อ่านเปรียบเทียบได้รวดเร็ว และพร้อมนำไปประกอบการวางแผน โดยไม่ใช้การตกแต่งที่แย่งความสนใจจากตัวเลข สถานะ และหลักฐานบนหน้าจอ

บุคลิกโดยรวมเป็นทางการ ทันสมัย และเป็นระบบ แต่ไม่แข็งกระด้าง พื้นผิวสว่าง เส้นขอบบาง และมุมโค้งช่วยให้ข้อมูลจำนวนมากยังเข้าถึงง่าย แถบข้างสีกรมท่าทำหน้าที่เป็นโครงถาวร ทีลเป็นสัญญาณของการกระทำและสถานะหลัก ส่วนชุดสีข้อมูลใช้แยกหมวดหมู่ในกราฟและตัวชี้วัดอย่างมีวินัย

**Key Characteristics:**
- โครงแอปแบบแถบข้างคงที่และพื้นที่ทำงานที่เลื่อนได้
- พื้นผิวขาวบนฉากหลังสเลตอ่อน แยกชั้นด้วยเส้นขอบมากกว่าเงา
- ทีลเป็นสีประจำระบบสำหรับการกระทำ สถานะเลือก และจุดนำสายตา
- สีข้อมูลหลายกลุ่มสงวนไว้สำหรับการจำแนกชุดข้อมูล ไม่ใช้แทนสีประจำระบบ
- ตัวอักษรระบบที่อ่านภาษาไทยได้ชัดเจน พร้อมความหนาและขนาดที่จัดลำดับข้อมูลตรงไปตรงมา

## Colors

ชุดสีใช้กรมท่าและสเลตสร้างความน่าเชื่อถือ ใช้ทีลเป็นเข็มทิศของการโต้ตอบ และใช้สเปกตรัมข้อมูลที่ควบคุมไว้เพื่อแยกหมวดหมู่ในกราฟ

### Primary
- **ทีลเข็มทิศข้อมูล** (`primary-action`): ใช้กับปุ่มหลัก แถบความคืบหน้า เส้นเน้น และการควบคุมที่ผู้ใช้ควรกระทำต่อ
- **ทีลสัญญาณสด** (`primary-bright`): ใช้กับไอคอน จุดเน้น และสถานะที่ต้องอ่านชัดบนพื้นกรมท่า
- **ทีลยืนยันการทำงาน** (`primary-active`): ใช้กับสถานะกด ลิงก์สำคัญ และข้อความทีลที่ต้องมีคอนทราสต์สูง
- **กระดาษทีลอ่อน** (`primary-soft`): ใช้เป็นพื้นหลังของชิป สถานะบัญชี และบริเวณเลือกที่ไม่ควรหนักเท่าปุ่มหลัก

### Secondary
- **น้ำเงินชุดข้อมูล** (`data-blue`): หนึ่งในสีจำแนกหมวดหมู่และชุดข้อมูลเชิงเปรียบเทียบ
- **อำพันชุดข้อมูล** (`data-amber`): ใช้กับหมวดกำลังคน คำเตือนแบบไม่วิกฤต และชุดข้อมูลที่ต้องแยกจากทีล
- **กุหลาบชุดข้อมูล** (`data-rose`): ใช้กับภาวะเสี่ยง ข้อมูล Thailand Zero Dropout และหมวดข้อมูลที่ต้องการความระมัดระวัง
- **ม่วงชุดข้อมูล** (`data-violet`): ใช้กับดัชนี การเปรียบเทียบ และหมวดข้อมูลที่ต้องแยกจากน้ำเงิน
- **ฟ้าทะเลชุดข้อมูล** (`data-cyan`): ใช้ขยายสเปกตรัมของกราฟที่มีหลายหมวดหมู่

### Neutral
- **กรมท่าโครงระบบ** (`system-navy`): พื้นหลังแถบข้างและฐานโครงสร้างที่ยึดทุกหน้าร่วมกัน
- **กระดาษงานข้อมูล** (`page-canvas`): ฉากหลังหลักของพื้นที่ทำงาน
- **พื้นผิวเอกสาร** (`surface`): การ์ด แถบหัว และคอนเทนเนอร์ที่วางข้อมูล
- **หมึกเน้น** (`ink-strong`): ตัวเลขสำคัญและค่าหลัก
- **หมึกเนื้อหา** (`ink-default`): ข้อความหลักและหัวเรื่องทั่วไป
- **หมึกอธิบาย** (`ink-muted`): คำอธิบาย ป้าย และข้อความประกอบ
- **หมึกบริบท** (`ink-subtle`): เมทาดาทา ข้อความรอง และสถานะที่ไม่ควรแย่งสายตา
- **เส้นแบ่งอ่อน** (`border-soft`): เส้นขอบการ์ด ตาราง และเส้นแบ่งส่วน
- **เส้นควบคุม** (`border-control`): เส้นขอบช่องกรอกข้อมูลและตัวเลือก

**The One System Accent Rule.** ทีลเป็นสีของระบบและการกระทำเสมอ สีข้อมูลอื่นใช้เพื่อจำแนกความหมายในกราฟ ตัวชี้วัด หรือสถานะเฉพาะ ไม่ใช้แทนปุ่มหลักแบบสุ่ม

**The Data Color Discipline Rule.** เมื่อใช้หลายสี ต้องมีคำอธิบายหรือบริบทที่ทำให้ผู้ใช้เข้าใจว่าสีแยกหมวดหมู่ใด ห้ามใช้สีหลายชุดเพื่อความสวยงามเพียงอย่างเดียว

## Typography

**Display Font:** System UI Sans (`ui-sans-serif`, `system-ui`, `Segoe UI`, Tahoma, sans-serif)
**Body Font:** System UI Sans (`ui-sans-serif`, `system-ui`, `Segoe UI`, Tahoma, sans-serif)

**Character:** ตัวอักษรแบบระบบให้บุคลิกเป็นทางการร่วมสมัย โหลดเร็ว และรองรับภาษาไทยโดยไม่เพิ่มภาระการดาวน์โหลด ลำดับชั้นเกิดจากขนาด น้ำหนัก และช่องว่าง ไม่พึ่งตัวอักษรตกแต่ง

### Hierarchy
- **Display** (700, `3rem`, line-height `1`): ใช้เฉพาะข้อความต้อนรับหรือหัวเรื่องหลักบนหน้าแรกเมื่อมีพื้นที่เพียงพอ
- **Headline** (600–700, `1.125rem`, line-height ประมาณ `1.75`): ใช้หัวข้อหมวดแดชบอร์ดและหัวเรื่องภายในส่วนสำคัญ
- **Title** (600, `0.875rem`, line-height `1.25`): ใช้ชื่อการ์ด ป้ายตัวกรอง และชื่อส่วนประกอบที่ต้องสแกนเร็ว
- **Body** (400, `1rem`, line-height `1.5`): ใช้เนื้อหาหลัก คำอธิบาย และข้อความทั่วไป
- **Label** (600, `0.75rem`, letter-spacing `0.025em`): ใช้เมทาดาทา ป้ายฟอร์ม และหัวข้อกลุ่มเมนู; ตัวพิมพ์ใหญ่สงวนไว้สำหรับคำอังกฤษสั้น ๆ เท่านั้น

**The Thai Legibility Rule.** ข้อความภาษาไทยต้องคง line-height ที่อ่านสระและวรรณยุกต์ได้ชัด ห้ามบีบระยะบรรทัดเพื่อเพิ่มข้อมูลบนหน้าจอ

**The Weight Before Decoration Rule.** สร้างลำดับด้วยน้ำหนัก 400, 500, 600 และ 700 ก่อนเพิ่มสีหรือองค์ประกอบตกแต่ง

## Layout

โครงเดสก์ท็อปใช้แถบข้างกว้าง `16rem` และแถบหัวสูง `4rem` พื้นที่เนื้อหาหลักเลื่อนได้อย่างอิสระ ภายในใช้กริดตอบสนองที่เริ่มจากหนึ่งคอลัมน์ แล้วเพิ่มเป็นสองถึงหกคอลัมน์ตามชนิดข้อมูลและความกว้างหน้าจอ จังหวะหลักใช้ช่วง `8px`, `12px`, `16px`, `20px`, `24px` และ `32px`; การ์ดข้อมูลมักเว้นช่อง `16–24px`

ที่ความกว้างต่ำกว่า `768px` แถบข้างเปลี่ยนเป็นเมนูนอกจอ กว้างไม่เกิน `18rem` หรือ `86vw` และมีฉากหลังทึบช่วยแยกชั้น ช่องควบคุมและปุ่มบนมือถือมีความสูงขั้นต่ำ `44px` ตารางกว้างใช้การเลื่อนแนวนอน และแผงตัวเลือกหลายรายการเปลี่ยนเป็นแผงลอยชิดขอบหน้าจอเพื่อรักษาพื้นที่สัมผัส

**The Working Canvas Rule.** เนื้อหาต้องเริ่มจากกริดหนึ่งคอลัมน์ที่อ่านได้ก่อน แล้วค่อยเพิ่มคอลัมน์เมื่อพื้นที่รองรับ ห้ามย่อการ์ดจนป้าย ตัวเลข หรือกราฟอ่านยาก

**The Operational Density Rule.** ใช้ความหนาแน่นระดับสบายถึงค่อนข้างแน่นสำหรับแดชบอร์ด แต่คงช่องว่างอย่างน้อยหนึ่งขั้นของสเกลระหว่างหัวข้อ ตัวควบคุม และผลลัพธ์

## Elevation & Depth

ระบบราบเรียบเป็นหลัก การ์ดและคอนเทนเนอร์แยกจากฉากหลังด้วยสีพื้นขาวและเส้นขอบสเลตอ่อน เงาบางระดับต่ำทำหน้าที่เพียงช่วยแยกพื้นผิว ไม่ทำให้การ์ดดูยกสูง เงาระดับกลางปรากฏเฉพาะเมื่อโฮเวอร์หรือเมื่อองค์ประกอบลอยเหนือเนื้อหา เช่น เมนูเลือกหลายรายการ แผงบนมือถือ และโมดอล

### Shadow Vocabulary
- **เส้นเงาแยกพื้นผิว** (`0 1px 2px rgba(15, 23, 42, 0.05)`): ใช้กับการ์ดข้อมูลที่ต้องแยกจากพื้นหลังโดยไม่สร้างมิติเด่น
- **เงาตอบสนอง** (`0 4px 6px -1px rgba(0, 0, 0, 0.10), 0 2px 4px -2px rgba(0, 0, 0, 0.10)`): ใช้เมื่อการ์ดที่คลิกได้ถูกโฮเวอร์เท่านั้น
- **เงาแผงลอย** (`0 12px 30px rgba(15, 23, 42, 0.16)`): ใช้กับเมนูหรือแผงที่วางทับเนื้อหา
- **เงาเมนูมือถือ** (`0 20px 45px rgba(15, 23, 42, 0.28)`): ใช้กับแถบข้างเมื่อเลื่อนเข้ามาบนหน้าจอขนาดเล็ก

**The Flat-at-Rest Rule.** พื้นผิวที่อยู่ในลำดับเอกสารปกติต้องดูราบเรียบ เส้นขอบและสีพื้นเป็นเครื่องมือหลัก เงาที่ชัดสงวนไว้สำหรับสถานะโต้ตอบหรือองค์ประกอบลอย

## Shapes

ภาษารูปทรงใช้มุมโค้งอย่างมีลำดับ: เมนูย่อยประมาณ `6px`, ปุ่มและช่องกรอกข้อมูล `8px`, การ์ด `12px`, และแผงลอยขนาดใหญ่ `16px` ชิปสถานะและตัวบ่งชี้แบบแคปซูลใช้มุมเต็ม เส้นขอบส่วนใหญ่หนา `1px`; เส้นหนา `2–4px` ใช้เป็นสัญญาณเลือกหรือเน้นหมวดเท่านั้น

**The Nested Radius Rule.** รัศมีขององค์ประกอบภายในต้องเล็กกว่าหรือเท่ากับพื้นผิวที่ครอบอยู่ เพื่อไม่ให้ฟอร์มและการ์ดดูซ้อนชั้นเกินจำเป็น

**The Border Before Shadow Rule.** หากเส้นขอบ `1px` แยกพื้นผิวได้เพียงพอ ห้ามเพิ่มเงาเพื่อแก้ปัญหาเดียวกัน

## Components

ส่วนประกอบมีบุคลิกเป็นทางการแต่เข้าถึงง่าย: รูปทรงชัด ป้ายตรงไปตรงมา สถานะสม่ำเสมอ และมีพื้นที่สัมผัสเพียงพอ

### Buttons
- **Shape:** มุมโค้งควบคุม (`8px`) และความสูงบนมือถืออย่างน้อย `44px`
- **Primary:** พื้นทีลเข็มทิศข้อมูล ตัวอักษรขาว น้ำหนัก 600 และระยะภายในประมาณ `12px 20px`
- **Hover / Focus:** โฮเวอร์เปลี่ยนเป็นทีลเข้มขึ้น; โฟกัสใช้เส้นทีลที่มองเห็นได้โดยไม่เปลี่ยนขนาดองค์ประกอบ
- **Secondary:** พื้นขาวหรือสเลตอ่อน เส้นขอบสเลต และข้อความสเลต ใช้กับยกเลิก รีเซ็ต หรือการกระทำรอง
- **Destructive:** ใช้กุหลาบเฉพาะการออกจากระบบ ความผิดพลาด หรือการกระทำที่มีความเสี่ยง ห้ามใช้แทนสีเน้นทั่วไป

### Chips
- **Style:** พื้นทีลอ่อน ข้อความทีลเข้ม เส้นขอบทีลอ่อน และมุมเต็ม
- **State:** ใช้แสดงสถานะผู้ใช้ บริบท หรือข้อมูลสั้น ๆ; ไม่ใช้แทนปุ่มหลักที่ต้องการการตัดสินใจชัดเจน

### Cards / Containers
- **Corner Style:** มุมการ์ด (`12px`)
- **Background:** พื้นผิวเอกสารสีขาวบนกระดาษงานข้อมูล
- **Shadow Strategy:** ราบเรียบที่สถานะพัก ใช้เพียงเส้นเงาแยกพื้นผิวระดับต่ำ
- **Border:** เส้นแบ่งอ่อน `1px`
- **Internal Padding:** ใช้ `18–24px` ตามความหนาแน่นของข้อมูล

### Inputs / Fields
- **Style:** พื้นสเลตอ่อน เส้นควบคุม `1px` มุม `8px` และระยะภายในประมาณ `10px 12px`
- **Focus:** เส้นขอบเปลี่ยนเป็นทีล พร้อมวงโฟกัสทีลโปร่งใสที่ไม่รบกวนการอ่าน
- **Error / Disabled:** ข้อผิดพลาดใช้กุหลาบพร้อมข้อความอธิบาย; สถานะปิดใช้ลดความทึบแต่ต้องยังอ่านป้ายได้

### Navigation
- แถบข้างใช้กรมท่าโครงระบบ ข้อความปกติเป็นสเลตอ่อน สถานะโฮเวอร์เพิ่มพื้นสเลต และสถานะเลือกใช้ทีลพร้อมเส้นนำด้านซ้าย
- เมนูหลักมีมุม `6px` ระยะภายใน `8px 12px` และไอคอนกว้างคงที่เพื่อให้ป้ายเรียงแนวเดียวกัน
- บนมือถือ เมนูเปลี่ยนเป็นแผงนอกจอพร้อมฉากหลังและปุ่มปิดที่มีพื้นที่สัมผัสชัดเจน

### Metric and Chart Cards
- ค่าหลักใช้หมึกเน้น ขนาดประมาณ `1.75–2rem` และวางเหนือคำอธิบายที่เบากว่า
- หัวการ์ดใช้ขนาด `0.875rem` น้ำหนัก 600; กราฟมีความสูงกำหนดชัดเพื่อไม่ให้เลย์เอาต์กระโดด
- สีกราฟมาจากชุดสีข้อมูลและต้องมี legend, ป้าย หรือบริบทที่อธิบายความหมาย

### Tables
- หัวตารางใช้พื้นสเลตอ่อนและสามารถตรึงด้านบนเมื่อรายการยาว
- ตัวเลขจัดชิดขวา ข้อความจัดชิดซ้าย เส้นแบ่งบาง และระยะเซลล์ประมาณ `8–12px`
- ตารางกว้างต้องเลื่อนแนวนอนบนจอเล็ก ห้ามย่อข้อความจนข้อมูลอ่านไม่ได้

## Do's and Don'ts

### Do:
- **Do** ใช้ทีลกับการกระทำหลัก สถานะเลือก และจุดนำสายตาที่มีความหมายต่อการทำงาน
- **Do** ใช้พื้นขาว เส้นขอบสเลตอ่อน และจังหวะ `16–24px` เพื่อแบ่งกลุ่มข้อมูล
- **Do** รักษาลำดับข้อความด้วยน้ำหนักและขนาดก่อนเพิ่มสี
- **Do** ให้กราฟ ตาราง และค่าตัวชี้วัดเป็นองค์ประกอบที่เด่นที่สุดในพื้นที่ทำงาน
- **Do** รักษาพื้นที่สัมผัสขั้นต่ำ `44px` และการเลื่อนตารางบนหน้าจอขนาดเล็ก
- **Do** ใช้ภาษาไทยที่ชัดเจนและคำอังกฤษเฉพาะคำย่อหรือศัพท์มาตรฐานที่ช่วยการทำงาน

### Don't:
- **Don't** ใช้สีของชุดข้อมูลเป็นสีปุ่มหลักหรือสีประจำระบบโดยไม่มีเหตุผลเชิงความหมาย
- **Don't** เพิ่มเงาชัดให้การ์ดทุกใบ; พื้นผิวปกติต้องราบเรียบและใช้เส้นขอบเป็นหลัก
- **Don't** ซ้อนการ์ดหลายชั้นหรือเพิ่มกรอบซ้ำรอบข้อมูลที่แยกกลุ่มชัดอยู่แล้ว
- **Don't** ใช้ตัวอักษรตกแต่ง ระยะบรรทัดแคบ หรือข้อความจางจนอ่านภาษาไทยยาก
- **Don't** ใช้สีเพียงอย่างเดียวเพื่อสื่อสถานะ; ต้องมีป้าย ไอคอน รูปแบบ หรือข้อความประกอบ
- **Don't** ลดขนาดตัวควบคุมหรือบีบกริดเพื่อรักษาจำนวนคอลัมน์บนมือถือ
