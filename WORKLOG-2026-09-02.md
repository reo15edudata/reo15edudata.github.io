# บันทึกส่งต่องาน — 2 กันยายน 2569

เอกสารนี้ใช้สำหรับย้ายงานไปทำต่อบนคอมพิวเตอร์เครื่องอื่น โดยสรุปสถานะล่าสุดของโครงการ `edu15-dashboard` สิ่งที่ดำเนินการแล้ว ผลตรวจ และประเด็นที่ยังต้องทำต่อ

## ภาพรวมผลิตภัณฑ์

- ชื่อระบบ: ระบบฐานข้อมูลด้านการศึกษา สำนักงานศึกษาธิการภาค 15
- กลุ่มผู้ใช้หลัก: นักวิชาการศึกษา นักวิเคราะห์นโยบายและแผน ผู้บริหารการศึกษา และหน่วยงานด้านการศึกษา
- พื้นที่รับผิดชอบ: เชียงใหม่ แม่ฮ่องสอน ลำพูน และลำปาง
- ภาษาหลัก: ภาษาไทย
- แหล่งข้อมูล: Google Sheets ผ่าน Google Apps Script
- สิทธิ์การใช้งาน:
  - ผู้ใช้ทั่วไปดูข้อมูลที่นำเสนอได้
  - ผู้ใช้ที่ลงชื่อเข้าใช้แก้ไขหรืออัปโหลดข้อมูลได้
- แนวทางหน้าแรก: เน้นภาพเปรียบเทียบ 4 จังหวัด ไม่เน้นการจัดอันดับความสดใหม่ของข้อมูล

รายละเอียดผลิตภัณฑ์และระบบออกแบบอยู่ใน `PRODUCT.md` และ `DESIGN.md`

## งานที่ดำเนินการแล้ววันนี้

### 1. ตรวจและปรับระบบ UI ทั้ง 12 หน้า

หน้าที่อยู่ในขอบเขต:

1. `index.html`
2. `dashboard.html`
3. `dashboard-index.html`
4. `dashboard-workforce.html`
5. `dashboard-score.html`
6. `dashboard-tzd.html`
7. `dashboard-business.html`
8. `dashboard-workforce-profile.html`
9. `admin.html`
10. `student-profile-form.html`
11. `tzd-upload.html`
12. `login.html`

งานสำคัญที่ปรับ:

- ทำรูปแบบ shell, sidebar, header, footer, spacing และ responsive behavior ให้เป็นระบบเดียวกัน
- ทำสถานะกำลังโหลด ข้อมูลว่าง เกิดข้อผิดพลาด ปุ่มลองใหม่ และสถานะพร้อมใช้งานให้สื่อสารชัดเจนขึ้น
- กำหนดชื่อและคำอธิบายกราฟก่อนเริ่มโหลดข้อมูล ลดการกระโดดของโครงหน้าและช่วยให้ผู้ใช้เข้าใจสิ่งที่กำลังรอ
- ปรับข้อความ ป้ายกำกับ และลำดับข้อมูลให้เหมาะกับผู้ใช้สายวางแผนการศึกษา
- แก้ label ของฟิลด์ `ntSubject`

### 2. หน้าแรกแบบเปรียบเทียบ 4 จังหวัด

- เพิ่มส่วนเปรียบเทียบเชียงใหม่ แม่ฮ่องสอน ลำพูน และลำปาง
- เปรียบเทียบจำนวนนักเรียน ครู และสถานศึกษา
- มีตัวเลือกปีการศึกษา สถานะกำลังโหลด สถานะผิดพลาด และปุ่มลองใหม่
- ไฟล์หลัก: `index.html`, `home.css`, `home-comparison.js`
- หน้าแรกเรียก `EDU15DataClient.fetchHomeProvinceSummary()`

### 3. Data client และ endpoint สรุป

- เพิ่มการเรียก endpoint แบบสรุปเพื่อลดการดึงข้อมูลดิบทั้งหมดมายังเบราว์เซอร์
- รองรับ `action=summary` และ `action=home-summary`
- มี fallback กลับไปคำนวณจากข้อมูลเดิม หาก backend เวอร์ชันเก่ายังไม่รองรับ endpoint ใหม่
- มี cache key แยกตามฐานข้อมูล ชีต ปี จังหวัด และตัวกรอง
- ไฟล์หลัก: `data-client.js`

ข้อควรระวังสำคัญ: `Code.gs` ไม่ได้อยู่ใน repository นี้ และ Code.gs ที่เคยนำมาแก้ก่อนหน้านี้ไม่ใช่เวอร์ชันล่าสุด ผู้ใช้ได้ส่งเวอร์ชันล่าสุดไว้เป็นไฟล์แนบ `pasted-text.txt` ของบทสนทนาเดิม ก่อนแก้หรือ deploy endpoint ฝั่ง Apps Script ต้องใช้ไฟล์แนบนั้นเป็นฐาน แล้วค่อยนำ handler ของ `summary` และ `home-summary` ไปผสาน ห้ามนำ Code.gs รุ่นเก่ามาทับ

### 4. ลดความเสี่ยงจากการสร้าง HTML และเพิ่มความทนทานของฟอร์ม

- เพิ่มการ escape ค่าที่มาจากข้อมูลก่อนนำไปประกอบ markup ในจุดสำคัญ
- เปลี่ยนจุดที่เหมาะสมให้ใช้ `textContent` หรือ DOM API
- จุดที่ยังใช้ `innerHTML` เป็น template ภายในระบบจะต้องคงหลักการ escape ค่าจากภายนอกก่อนเสมอ
- เพิ่ม autosave draft และกู้คืนร่างให้:
  - แบบฟอร์มโปรไฟล์ผู้สนใจฝึกประสบการณ์
  - แบบกรอกข้อมูล Thailand Zero Dropout
- ร่างมีอายุจำกัดและไม่บันทึกรหัส OTP
- หลังบันทึกสำเร็จมีการล้างร่าง
- ไฟล์หลัก: `student-profile-form.js`, `tzd-upload.js`

### 5. ปรับประสิทธิภาพการโหลด

- แยกการโหลดไลบรารีตามการใช้งานของหน้า แทนการให้ทุกหน้าดึงไลบรารีทั้งหมด
- เพิ่มตัวช่วยโหลด Chart.js, chartjs-plugin-datalabels, Leaflet และ XLSX ตามความจำเป็น
- ลดการร้องขอข้อมูลซ้ำด้วย cache และคำขอสรุป
- ไฟล์หลัก: `site.js`, `data-client.js` และไฟล์ dashboard แต่ละชุด

### 6. ระบบสีและ WCAG AA

- รวมสีหลักเป็น semantic tokens กลางใน `responsive.css`
- กลุ่ม token ประกอบด้วย canvas, surface, ink, border, action, info, success, warning, danger, focus และ data palette
- สี action หลักเปลี่ยนเป็น `#0f766e` เพื่อให้ข้อความสีขาวบนปุ่มผ่าน AA
- สีข้อความรอง เส้นขอบฟอร์ม เมนูด้านข้าง footer และสถานะต่าง ๆ ถูกปรับคอนทราสต์
- กราฟและแผนที่เรียกสีผ่าน `window.EDU15Theme` ใน `site.js`
- ปรับ `home.css`, `footer.js` และหน้า login ให้ใช้ token ชุดเดียวกัน
- อัปเดตคำอธิบายระบบสีใน `DESIGN.md`
- ทุกหน้าที่ใช้ `responsive.css` อ้างเวอร์ชัน `v=20260902-2`

## ผลการตรวจล่าสุด

- เปิดตรวจหน้าเว็บจริงครบ 12 หน้าใน local server
- การตรวจ computed foreground/background ของข้อความและส่วนควบคุมที่มองเห็น: ไม่พบรายการต่ำกว่า WCAG AA หลังแก้ไขรอบสุดท้าย
- สีข้อความปกติใช้เกณฑ์อย่างน้อย 4.5:1
- ข้อความขนาดใหญ่ใช้เกณฑ์อย่างน้อย 3:1
- เส้นขอบส่วนควบคุมหลักใช้สี `#7c8da3` ซึ่งผ่านเกณฑ์ 3:1 บนพื้นขาวและพื้น canvas
- JavaScript ที่เกี่ยวข้องกับ theme และ dashboard ผ่านการตรวจ syntax
- `git diff --check` ผ่าน มีเพียงคำเตือนเรื่อง Git จะเปลี่ยน LF เป็น CRLF บน Windows

## สถานะ Git ตอนบันทึก

- งานทั้งหมดในวันนี้ยังไม่ได้ commit
- working tree มีทั้งไฟล์แก้ไขและไฟล์ใหม่
- ไฟล์ใหม่สำคัญ ได้แก่:
  - `PRODUCT.md`
  - `DESIGN.md`
  - `home.css`
  - `home-comparison.js`
- โฟลเดอร์ `.agents/`, `.codex/` และ `.impeccable/` ยังเป็น untracked ต้องพิจารณาก่อน commit ว่าจะเก็บใน repository หรือเพิ่มใน `.gitignore`
- ห้ามใช้ `git reset --hard` หรือ checkout ทับไฟล์ เพราะการเปลี่ยนแปลงยังไม่ได้ commit

## วิธีเริ่มงานต่อบนเครื่องใหม่

1. คัดลอกทั้งโฟลเดอร์โครงการ รวมไฟล์ที่ยังไม่ถูก track และโฟลเดอร์ `.git`
2. ตรวจว่าการเปลี่ยนแปลงมาครบด้วย `git status --short`
3. ติดตั้ง dependencies ด้วย `pnpm install`
4. หากแก้ Tailwind utilities ให้สร้าง CSS ใหม่ด้วย `pnpm run build:css`
5. เปิดเว็บผ่าน local HTTP server ห้ามเปิดไฟล์ HTML ด้วย `file://`
6. ตรวจหน้าเว็บทั้ง 12 หน้าก่อน commit โดยเฉพาะหน้าแรก dashboard และสองแบบฟอร์มอัปโหลด
7. ก่อนทำ backend ต่อ ให้นำ Code.gs เวอร์ชันล่าสุดจากไฟล์แนบเดิมมาเก็บเป็นไฟล์ทำงาน แล้วจึงผสาน endpoint ใหม่

## งานที่ควรทำต่อ

ลำดับแนะนำ:

1. ผสาน `summary` และ `home-summary` เข้ากับ Code.gs เวอร์ชันล่าสุดจากผู้ใช้
2. ทดสอบ endpoint กับ Google Sheets จริงทั้ง 4 จังหวัด รวมกรณีปีว่าง จังหวัดไม่มีข้อมูล และชื่อคอลัมน์ไม่ตรง
3. ทดสอบสิทธิ์ผู้ใช้ทั่วไป ผู้ใช้ที่ลงชื่อเข้าใช้ และการหมดอายุของ session
4. ทดสอบ autosave draft, restore, submit สำเร็จ และการล้างร่างบนเบราว์เซอร์จริง
5. ตรวจหน้าจอมือถือและแท็บเล็ตทุกหน้า รวม sidebar, ตารางกว้าง และฟอร์มจำนวนมาก
6. ตรวจ network requests เพื่อยืนยันว่าไลบรารีถูกโหลดเฉพาะหน้าที่ใช้จริง
7. แยก commit ตามหัวข้อ เช่น UI system, homepage comparison, data endpoint client, form hardening และ color tokens

## จุดอ้างอิงสำคัญในโค้ด

- Tokens กลางและ responsive rules: `responsive.css`
- Theme API, shell, loader และ lazy library loader: `site.js`
- Data API, cache และ summary fallback: `data-client.js`
- หน้าแรกเปรียบเทียบจังหวัด: `home-comparison.js`
- แบบฟอร์มโปรไฟล์และ draft: `student-profile-form.js`
- แบบกรอก TZD และ draft: `tzd-upload.js`
- หลักการผลิตภัณฑ์: `PRODUCT.md`
- หลักการออกแบบ: `DESIGN.md`

## หมายเหตุสำหรับผู้ที่รับช่วงต่อ

- รักษาภาษาไทยเป็นภาษาหลักและใช้คำที่เจ้าหน้าที่การศึกษาเข้าใจได้ทันที
- หน้าแรกต้องเน้นการเปรียบเทียบ 4 จังหวัด ไม่เปลี่ยนกลับไปเน้นความสดใหม่ของฐานข้อมูล
- สี teal ใช้กับ action, สี slate ใช้กับโครงสร้างและข้อความ ส่วนสีอื่นใช้กับสถานะหรือข้อมูลในกราฟ
- เมื่อเพิ่มสีใหม่ ให้เพิ่มเป็น token ใน `responsive.css` ก่อน หลีกเลี่ยงการใส่ค่าสีซ้ำในแต่ละไฟล์
- อย่าลบ fallback ของ summary endpoint จนกว่า Code.gs รุ่นใหม่จะ deploy และผ่านการทดสอบจริงแล้ว
