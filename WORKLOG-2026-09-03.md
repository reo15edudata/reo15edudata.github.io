# บันทึกส่งต่องาน — 3 กันยายน 2569

## งานที่ดำเนินการ

- ปรับพื้นที่กดบนมือถือและอุปกรณ์สัมผัสให้มีขนาดอย่างน้อย 44×44px
- เพิ่ม token กลาง `--edu-touch-target: 2.75rem` ใน `responsive.css`
- ครอบคลุมปุ่ม ช่องกรอก select, textarea, summary, controls ที่ใช้ ARIA และพื้นที่เลื่อนที่รับโฟกัส
- ขยายพื้นที่กดของปุ่มเปิดเมนู ปุ่มแบ่งหน้า ลิงก์ใน header/sidebar ลิงก์รายละเอียดหน้าแรก ลิงก์ WEF ในแบบฟอร์ม และลิงก์ติดต่อใน footer
- เพิ่มความสูงของ label ที่ครอบ checkbox/radio เพื่อให้แตะได้สะดวก โดยไม่ขยายตัว checkbox จนผิดรูป
- ใช้ media query สำหรับหน้าจอไม่เกิน 767px และอุปกรณ์ที่มี `pointer: coarse` เพื่อรองรับโทรศัพท์ แท็บเล็ต และโน้ตบุ๊กจอสัมผัส
- อัปเดต cache version ของ `responsive.css` เป็น `v=20260903-1` ครบทั้ง 12 หน้า

## ผลตรวจ

- ตรวจหน้าเว็บจริงที่ viewport 390×844px ครบทั้ง 12 หน้า
- ไม่พบส่วนควบคุมที่มองเห็นและมีพื้นที่กดต่ำกว่า 44×44px หลังแก้ไข
- ตรวจภาพหน้าแรกบนมือถือแล้ว เมนู drawer และเนื้อหาหลักยังจัดวางถูกต้อง
- คืน viewport เป็นเดสก์ท็อปและยืนยันว่าอุปกรณ์แบบเมาส์ยังคงใช้ขนาดควบคุมแบบกะทัดรัด

## ไฟล์ที่แก้ไขในงานนี้

- `responsive.css`
- HTML ทั้ง 12 หน้า เพื่ออัปเดต cache version

## หมายเหตุ

- ควรทดสอบเพิ่มเติมบน iPhone และ Android จริงอย่างละหนึ่งเครื่อง เพราะ browser emulation ไม่สามารถจำลองพฤติกรรมการแตะและ browser chrome ได้ครบทั้งหมด
- สถานะ Git ของโครงการยังมีการเปลี่ยนแปลงจากงานก่อนหน้าที่ยังไม่ได้ commit ห้าม reset หรือ checkout ทับ

---

## งาน Typeset — ระบบตัวอักษรกลาง

- เพิ่ม typography tokens กลางใน `responsive.css` สำหรับ metadata, label, body, card title, section title, page title และ display
- กำหนดข้อความเนื้อหาหลักไว้ที่ 16px พร้อม line-height 26px และเพิ่มระยะบรรทัดที่เหมาะกับภาษาไทย
- ยกระดับข้อความประกอบและ metadata เป็นอย่างน้อย 13px บนเดสก์ท็อป และ 14px บนมือถือ
- แยกลำดับชั้นหัวข้อ `h1`, `h2`, `h3` ให้ต่างกันชัดเจน โดยไม่กระทบหัวข้อที่ระบุขนาดไว้เฉพาะ
- ปรับข้อความขนาดเล็กในหน้าเปรียบเทียบ 4 จังหวัด หน้าคะแนน โปรไฟล์ผู้เรียน แบบฟอร์ม และหน้าอัปโหลดให้ใช้ scale กลาง
- รวมแบบอักษรของหน้าเข้าสู่ระบบและหน้าหลักให้ใช้ system sans ชุดเดียวกัน
- กำหนดตัวเลขในตารางและการ์ดตัวชี้วัดให้ใช้ tabular numerals เพื่อช่วยให้เปรียบเทียบค่าได้ง่าย
- ปรับข้อความและ data label ภายในกราฟให้ไม่เล็กเกินไป พร้อมตั้งค่าแบบอักษรของ Chart.js ให้ตรงกับระบบ
- อัปเดต `DESIGN.md` ให้บันทึก typography scale ใหม่ และเพิ่ม cache version ของ `responsive.css` เป็น `v=20260903-3` ครบทั้ง 12 หน้า

## ผลตรวจ Typeset

- ตรวจหน้าเว็บจริงทั้งเดสก์ท็อป 1280px และมือถือ 390×844px ครบทั้ง 12 หน้า
- ไม่พบ horizontal overflow หรือข้อความที่ถูกตัดจากการเพิ่มขนาดตัวอักษร
- จุดที่ยังใช้ glyph ขนาด 8–10px เป็นไอคอนตกแต่ง ไม่ใช่ข้อความสำหรับอ่าน
- ตัวตรวจอัตโนมัติทำงานแบบลดความสามารถเนื่องจากไม่มี parser เสริมในเครื่อง จึงยืนยันผลด้วย computed styles และการตรวจหน้าเว็บจริงร่วมกัน

---

## งาน Polish — ตรวจภาพและ Interaction รอบส่งมอบ

- ตรวจหน้าเว็บครบทั้ง 12 หน้าที่ความกว้าง 1280px, 768px และ 390px
- ตรวจหน้าแรก แดชบอร์ดขณะเปิดเมนูมือถือ แบบฟอร์มผู้เรียน และตารางกรอกข้อมูล TZD ด้วยภาพหน้าเว็บจริง
- เพิ่มชื่อสำหรับโปรแกรมอ่านหน้าจอให้ปุ่มกรอกข้อมูล TZD และปุ่มย้อนกลับในแบบฟอร์มผู้เรียน ซึ่งเหลือเพียงไอคอนบนมือถือ
- เพิ่มสถานะ `aria-expanded` ให้ปุ่มย่อ–ขยายแถบเมนูบนเดสก์ท็อป และอัปเดตสถานะเมื่อย่อหรือขยาย
- อัปเดต cache version ของ `site.js` เป็น `v=20260903-2` ในหน้าที่ใช้งาน

## ผลตรวจ Polish

- ไม่พบ horizontal overflow, duplicate ID, รูปที่ไม่มี alt หรือ console error ในรอบตรวจ
- เมนูมือถือเปิด ปิดด้วยปุ่ม และปิดด้วย Escape ได้ พร้อมคืนสถานะ ARIA ถูกต้อง
- โมดอลวิธีอ่านและคำนวณเปิดแบบ modal และปิดด้วยปุ่มหรือ Escape ได้
- โหมดแก้ไขข้อมูล TZD แสดงช่องเลือกรอบเดิมและปิดช่องรอบปัจจุบันถูกต้อง
- ลำดับโฟกัสด้วยแป้นพิมพ์มองเห็นเส้น focus ring ขนาด 3px
- แบบฟอร์มยังระบุช่องบังคับช่องแรกได้ และไม่มีการส่งข้อมูลจริงระหว่างการทดสอบ
