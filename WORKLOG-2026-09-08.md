# บันทึกส่งต่องาน — 8 กันยายน 2569

เอกสารนี้สรุปสถานะล่าสุดของงานระบบรับข้อมูลจากบุคคลภายนอก ระบบตรวจสอบข้อมูล และการปรับหน้า Dashboard เพื่อใช้ทำงานต่อบนคอมพิวเตอร์อีกเครื่อง

## ขอบเขตที่ตกลงกันล่าสุด

- หยุดนำเสนอ Dashboard Thailand Zero Dropout ชั่วคราว โดยซ่อนทางเข้าจาก Sidebar แต่ยังเก็บ `dashboard-tzd.html`, `dashboard-tzd.js`, `tzd-upload.html` และ `tzd-upload.js` ไว้
- เพิ่มช่องทางให้เจ้าของสถานประกอบการส่งข้อมูลโดยไม่ต้องลงทะเบียน แต่ต้องยืนยันอีเมลด้วย OTP
- ข้อมูลสถานประกอบการและโปรไฟล์ผู้เรียนที่ส่งจากบุคคลภายนอกต้องอยู่ในสถานะรอตรวจสอบ และยังไม่ควรเผยแพร่ทันที
- เพิ่มระบบตรวจสอบข้อมูลใน Admin Console รองรับอนุมัติ ขอแก้ไข และไม่อนุมัติ
- ต้องมีหน้าสำหรับผู้ส่งใช้ตรวจสอบสถานะและกลับมาแก้ไขข้อมูล โดยอนุญาตเฉพาะอีเมลเดียวกับที่ใช้ส่งครั้งแรก

## งานหน้าเว็บที่ดำเนินการแล้ว

### 1. แบบฟอร์มสถานประกอบการ

- สร้าง `business-submission-form.html`, `business-submission-form.js` และ `submission-form.css`
- รองรับข้อมูลจังหวัด ประเภทและชื่อสถานประกอบการ พิกัด รายละเอียด ค่าตอบแทน ทักษะที่ต้องการ และช่องทางติดต่อ
- ปีข้อมูลตั้งเป็นปี พ.ศ. ปัจจุบันโดยอัตโนมัติ
- พิกัดรองรับทั้งการค้นหาสถานที่และการปักหมุดบนแผนที่
- ทักษะอ้างอิง WEF Skills Taxonomy แสดงภาษาไทยเป็นหลัก แต่ส่งชื่อภาษาอังกฤษมาตรฐานไปยังฐานข้อมูล สูงสุด 10 ทักษะ
- รองรับ OTP ทางอีเมลและแสดงรหัสติดตามหลังส่งข้อมูลสำเร็จ
- แก้ช่องค่าตอบแทนไม่ให้ข้อความหน่วย `บาท/ชั่วโมง` ถูกบีบจนอ่านไม่ชัดแล้ว

### 2. ระบบตรวจสอบข้อมูลใน Admin Console

- เพิ่มเมนูและหน้าตรวจสอบข้อมูลใน `admin.html`, `admin.js` และ `admin-moderation.css`
- แยกรายการเป็นข้อมูลสถานประกอบการและโปรไฟล์ผู้เรียน
- มีจำนวนสรุปตามสถานะ ตัวกรองสถานะ ช่องค้นหา ตารางรายละเอียด และการแบ่งหน้า
- รองรับ checkbox เลือกหลายรายการเพื่ออนุมัติ ขอแก้ไข หรือไม่อนุมัติพร้อมกัน
- การขอแก้ไขและไม่อนุมัติต้องระบุหมายเหตุถึงผู้ส่ง
- หน้าเว็บเรียก Apps Script ด้วย action `getSubmissionsForReview` และ `reviewSubmissions`

### 3. Schema และแบบฟอร์มโปรไฟล์ผู้เรียน

- ปรับ schema ฝั่งหน้าเว็บให้รองรับ `BUSINESS_CODE`, `STUDENT_CODE` และ `DATA_STATUS`
- แบบฟอร์มโปรไฟล์ผู้เรียนส่งข้อมูลเข้าสถานะตรวจสอบและแสดงรหัสติดตามเมื่อสำเร็จ
- ปรับส่วน WEF Skills ให้แสดงชื่อภาษาไทยเป็นหลัก แต่เก็บชื่อภาษาอังกฤษมาตรฐานใน `TOP_SKILLS`
- ตัดการ์ด “ความพร้อมก่อนส่งข้อมูล” ออกจาก `student-profile-form.html` แล้ว
- ลบ JavaScript ที่คำนวณและแสดงการ์ดดังกล่าวแล้ว โดยระบบตรวจช่องบังคับ OTP และทักษะยังทำงานตามเดิม
- ระบบบันทึกและกู้คืนร่างในอุปกรณ์ยังทำงานแบบไม่แสดงสถานะ

### 4. Dashboard สถานประกอบการ

- เพิ่มปุ่ม “เพิ่มข้อมูลสถานประกอบการ” และปรับสีให้เห็นชัดจากพื้นหลัง
- เพิ่มตัวกรองประเภทสถานประกอบการและช่องค้นหาชื่อสถานประกอบการ
- แก้รายการปีให้รวมปี พ.ศ. ปัจจุบัน และบังคับดึง metadata ใหม่เพื่อลดปัญหาปี 2569 ไม่ปรากฏจาก cache เก่า

## งานฝั่ง Apps Script และฐานข้อมูล

- `Code.gs` เวอร์ชันล่าสุดไม่ได้เก็บอยู่ใน repository นี้ ต้องใช้โค้ดจาก Apps Script project ที่ deploy อยู่เป็นแหล่งข้อมูลหลัก ห้ามนำ Code.gs รุ่นเก่ามาทับ
- ระบบใหม่พึ่งพาชีต `Submission_Registry` สำหรับเก็บอีเมล รหัสติดตาม สถานะ หมายเหตุ และข้อมูลประกอบการตรวจสอบ
- เคยพบข้อผิดพลาดว่าหัวตาราง `Submission_Registry` ไม่ตรง schema เนื่องจากยังไม่มีชีตนี้ จึงต้องยืนยันอีกครั้งว่าชีตถูกสร้างด้วยชื่อคอลัมน์ ลำดับ และจำนวนคอลัมน์ตรงกับ Code.gs เวอร์ชันที่ deploy อยู่
- หลังแก้ Code.gs ต้องสร้าง Deployment เวอร์ชันใหม่ และให้หน้าเว็บใช้ URL `/exec` ของ deployment ที่ถูกต้อง
- OTP ของโปรไฟล์ผู้เรียนเคยทดสอบส่งสำเร็จแล้วหลังเพิ่มสิทธิ์ `script.send_mail` แต่ flow ใหม่ของสถานประกอบการและระบบอนุมัติยังควรทดสอบครบวงจรอีกครั้ง

## งานที่ต้องทำต่อ — เรียงตามลำดับ

> อัปเดตล่าสุด: ผู้ใช้ทดสอบขั้นที่ 1–2 ครบแล้ว ระบบกรอกข้อมูลและการอนุมัติ/ขอแก้ไข/ไม่อนุมัติทำงานจริง ขั้นที่ 3 ดำเนินการกรองข้อมูลสาธารณะแล้ว และขั้นที่ 4 พัฒนาเสร็จในไฟล์งาน รอ Deploy Apps Script กับ GitHub Pages เพื่อทดสอบครบวงจรบนระบบจริง

### ขั้นที่ 1: ทดสอบระบบรับข้อมูลครบวงจร

1. ส่งโปรไฟล์ผู้เรียนหนึ่งรายการและข้อมูลสถานประกอบการหนึ่งรายการด้วยอีเมลทดสอบ
2. ยืนยันว่า OTP ใช้งานได้ และทั้งสองรายการได้รับรหัส `STUDENT_CODE`/`BUSINESS_CODE`
3. ตรวจว่ารายการใหม่มี `DATA_STATUS` เป็น `PENDING`
4. ตรวจว่า `Submission_Registry` มีรายการและเก็บอีเมลสำหรับตรวจสอบสิทธิ์ในอนาคต
5. ทดสอบรูปแบบรหัสปี พ.ศ. + ประเภท + cluster สองหลัก + ลำดับสามหลัก รวมถึงกรณีข้ามจากลำดับ 999 ไป cluster ถัดไป

### ขั้นที่ 2: ทดสอบ Admin Console

1. เข้าระบบด้วยบัญชีผู้ดูแลและเปิดเมนูตรวจสอบข้อมูล
2. ทดสอบดูรายละเอียด ค้นหา กรองสถานะ เลือกทีละรายการ และเลือกหลายรายการ
3. ทดสอบ `APPROVED`, `REVISION_REQUIRED` และ `REJECTED`
4. ตรวจว่าสถานะใน `Submission_Registry` และ `DATA_STATUS` ในชีตปลายทางเปลี่ยนตรงกัน
5. ตรวจว่าหมายเหตุของผู้ดูแลถูกบันทึกและอ่านกลับมาได้

### ขั้นที่ 3: ป้องกันข้อมูลที่ยังไม่อนุมัติไม่ให้เผยแพร่

- ตรวจ endpoint อ่านข้อมูลสาธารณะของ `Vocational_Busi_MOU` และ `Business_Student_Profile`
- ให้ Dashboard แสดงเฉพาะแถวที่ `DATA_STATUS` เป็น `APPROVED`
- ข้อมูลเดิมที่สร้างก่อนมีระบบสถานะต้องกำหนดนโยบายให้ชัดว่าจะถือเป็นอนุมัติแล้ว หรือเติมค่า `APPROVED` ลงฐานข้อมูลก่อนเปิดใช้จริง
- ห้ามอาศัยการซ่อนข้อมูลด้วย JavaScript ฝั่งเบราว์เซอร์เพียงอย่างเดียว ควรกรองตั้งแต่ Apps Script ก่อนส่งข้อมูลสาธารณะ

### ขั้นที่ 4: สร้างหน้าตรวจสอบสถานะและแก้ไขข้อมูล

- สร้าง `submission-status.html`, `submission-status.css` และ `submission-status.js` แล้ว
- ผู้ส่งค้นหาด้วยรหัสติดตามและอีเมลเดิม จากนั้นต้องยืนยัน OTP ก่อนเห็นรายละเอียด
- แสดงสถานะกำลังพิจารณา อนุมัติแล้ว ขอแก้ไข และไม่อนุมัติ พร้อมหมายเหตุจากผู้ดูแล
- เปิดแบบฟอร์มแก้ไขเฉพาะสถานะ `REVISION_REQUIRED` และตรวจสิทธิ์ซ้ำใน Apps Script ก่อนบันทึก
- การส่งแก้ไขใช้รหัสเดิม อัปเดตแถวเดิม เปลี่ยนสถานะกลับเป็น `PENDING` และบันทึกเวลาอัปเดตล่าสุด
- เพิ่มทางเข้าหน้าตรวจสอบสถานะในแบบฟอร์ม หน้าสำเร็จหลังส่ง และ Dashboard ที่เกี่ยวข้องแล้ว
- รอ Deploy และทดสอบ OTP/การแก้ไขกับ Google Sheet จริง

### ขั้นที่ 5: ตรวจ UI และอุปกรณ์จริง

- ทดสอบแบบฟอร์มสถานประกอบการบนมือถือ โดยเฉพาะแผนที่ ตัวเลือก WEF ช่องค่าตอบแทน และปุ่มส่ง
- ทดสอบตารางตรวจสอบข้อมูลใน Admin Console บนหน้าจอแคบและรายการจำนวนมาก
- ทดสอบ Dashboard สถานประกอบการว่าปี 2569 ประเภท และการค้นหาชื่อทำงานร่วมกันถูกต้อง
- ตรวจ console error และ network request ของหน้าที่แก้ไขทั้งหมด

### ระบบ Job Matching

- เพิ่ม `job-matching.html`, `job-matching.css` และ `job-matching.js`
- รองรับสองบทบาท: ผู้เรียนค้นหาสถานประกอบการ และสถานประกอบการค้นหาผู้เรียน
- ใช้เฉพาะข้อมูล `APPROVED` และแสดง 5 อันดับที่มีคะแนนสูงสุด
- คะแนนเต็ม 100 คะแนน แบ่งเป็นทักษะ WEF 65 คะแนน จังหวัด 25 คะแนน และงาน/บริบท 10 คะแนน
- แบ่งระดับ A–D พร้อมสี เหตุผลย่อ คะแนนรายด้าน ทักษะที่ตรงกัน และรายละเอียดที่คลิกอ่านได้
- เพิ่มทางเข้าจากหน้า Dashboard ความต้องการกำลังคนและเมนูย่อยใน Sidebar
- เพิ่ม `PROV_NAME` ใน schema แบบฟอร์ม การแก้ไขข้อมูล ตัวกรอง และรายละเอียดโปรไฟล์ผู้เรียน
- เพิ่มช่องค้นหารายชื่อแบบพิมพ์กรองได้ทั้งชื่อ จังหวัด และบริบทที่แสดงในรายการเลือก
- แก้แผงรายละเอียดให้ซ่อนข้อความสถานะว่างทันทีเมื่อเปิดผลลัพธ์ ไม่ทำให้ข้อมูลใหม่ถูกเบียดลงด้านล่าง
- จัดทำไฟล์ Mock-up `BUSINESS_WANTS_WEF_Mock_756.xlsx` จำนวน 756 แถว แต่ละแถวสุ่มทักษะ WEF 3–4 รายการโดยไม่ซ้ำกันภายในช่อง
- เพิ่มช่องค้นหารายชื่อด้วยการพิมพ์ รองรับทั้งรายชื่อผู้เรียนและสถานประกอบการ พร้อมแสดงจำนวนผลลัพธ์ที่พบ
- แก้แผงรายละเอียดให้ซ่อนคำแนะนำเริ่มต้นทันทีเมื่อคลิกผลลัพธ์ จึงไม่เบียดรายละเอียดลงด้านล่าง
- จัดทำไฟล์ Mock-up `BUSINESS_WANTS` จำนวน 756 แถว โดยสุ่มทักษะ WEF แถวละ 3–4 ทักษะสำหรับทดสอบการจับคู่

## สถานะ Git ตอนบันทึก

- Branch ปัจจุบัน: `main`
- Remote: `origin` ชี้ไปที่ `https://github.com/reo15edudata/reo15edudata.github.io.git`
- working tree ยังมีไฟล์แก้ไขและไฟล์ใหม่จำนวนมาก รวมทั้งหมด 43 รายการ (นับรวม Worklog ฉบับนี้) และยังไม่ได้ commit ในงานรอบนี้
- ไฟล์ใหม่สำคัญของระบบล่าสุด ได้แก่ `business-submission-form.html`, `business-submission-form.js`, `submission-form.css`, `admin-moderation.css` และ `dashboard-business.css`
- โฟลเดอร์ `.agents/`, `.codex/` และ `.impeccable/` ยังเป็น untracked ต้องพิจารณาว่าจะ commit หรือเพิ่มใน `.gitignore` ก่อนใช้ `git add .`
- ห้ามใช้ `git reset --hard` หรือ checkout ทับไฟล์ เพราะจะทำให้งานที่ยังไม่ได้ commit สูญหาย

## ก่อนย้ายไปทำงานบนคอมพิวเตอร์อีกเครื่อง

- การแก้ไขที่ยังไม่ commit/push จะไม่ปรากฏบนอีกเครื่องจากการ `git pull`
- ก่อนปิดเครื่องนี้ ต้อง commit และ push ไฟล์งานที่ต้องการส่งต่อ หรือคัดลอกทั้งโฟลเดอร์รวมไฟล์ untracked และ `.git` ไปยังอีกเครื่อง
- ถ้าจะใช้ Git เป็นวิธีส่งต่อ ให้ตรวจรายการไฟล์ที่ไม่ควรขึ้น repository โดยเฉพาะ `.codex/`, `.impeccable/`, ไฟล์แนบ และข้อมูลลับ ก่อน staging
- บนอีกเครื่องให้ตรวจ `git status --short` ก่อนเริ่มงาน และอย่าแก้ไฟล์เดียวกันค้างไว้สองเครื่องพร้อมกัน
- ติดตั้ง pnpm ผ่าน Node.js/Corepack หากคำสั่ง `pnpm` ยังไม่พร้อม จากนั้นใช้ `pnpm install`
- หากแก้ Tailwind utilities ให้ใช้ `pnpm run build:css` ก่อนทดสอบและ commit

## ไฟล์หลักสำหรับเริ่มงานต่อ

- แบบฟอร์มสถานประกอบการ: `business-submission-form.html`, `business-submission-form.js`, `submission-form.css`
- โปรไฟล์ผู้เรียน: `student-profile-form.html`, `student-profile-form.js`, `dashboard-workforce-profile.html`, `dashboard-workforce-profile.js`
- Dashboard สถานประกอบการ: `dashboard-business.html`, `dashboard-business.js`, `dashboard-business.css`
- ระบบตรวจสอบ: `admin.html`, `admin.js`, `admin-moderation.css`
- Schema สำหรับอัปโหลด: `app.js`
- Data API และการกรองข้อมูลสาธารณะ: `data-client.js`
- Sidebar และสถานะระบบ: `site.js`
- Backend: Code.gs เวอร์ชันล่าสุดใน Google Apps Script project ที่ deploy อยู่
